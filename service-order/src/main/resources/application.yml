server:
  port: 8081

spring:
  r2dbc:
    url: r2dbc:mysql://localhost:3306/orders?zeroDateTimeBehavior=convertToNull
    username: root
    password: secret
    pool:
      enabled: true
      max-size: 20
      initial-size: 5
      max-idle-time: 30m
  sql:
    init:
      mode: always